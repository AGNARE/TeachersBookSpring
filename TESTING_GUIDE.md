# 🧪 Руководство по тестированию TeachersBook

## 🚀 Быстрый старт

### 1. Запуск приложения

**Backend:**
```bash
cd web
.\mvnw.cmd spring-boot:run
```
✅ Должен запуститься на http://localhost:8181

**Frontend:**
```bash
cd frontend
cmd /c npm run dev
```
✅ Должен запуститься на http://localhost:5173

---

## 🎯 Приоритетное тестирование (30 минут)

### Шаг 1: Smoke Test (5 мин)
Проверьте, что система работает:

1. Откройте http://localhost:5173
2. Войдите: `admin` / `admin`
3. Убедитесь, что Dashboard загрузился
4. Нажмите "Выйти"
5. Убедитесь, что вернулись на Login

**Результат:** ✅ Базовая функциональность работает

---

### Шаг 2: Управление пользователями (10 мин)

1. Войдите как admin
2. Перейдите в "Управление пользователями"
3. Создайте преподавателя:
   - Нажмите "+ Создать"
   - Имя: `Тест`
   - Фамилия: `Преподаватель`
   - Роль: `Преподаватель`
   - Нажмите "Сгенерировать логин/пароль"
   - Запишите логин и пароль
   - Нажмите "Создать"

4. Проверьте вкладку "Преподаватели" - новый пользователь должен появиться

5. Создайте студента аналогично

6. Выйдите и войдите с учетными данными нового преподавателя

**Результат:** ✅ Создание и аутентификация пользователей работает

---

### Шаг 3: Проверка Dashboard (5 мин)

1. Войдите как admin
2. На Dashboard проверьте:
   - Отображаются ли карточки статистики
   - Есть ли данные о студентах, группах
   - Загружается ли секция "Ближайшие занятия"

**Результат:** ✅ Dashboard отображает актуальные данные

---

### Шаг 4: API тесты через curl (10 мин)

**Тест 1: Аутентификация**
```bash
curl -X POST http://localhost:8181/api/auth/login \
  -H "Content-Type: application/json" \
  -d "{\"username\":\"admin\",\"password\":\"admin\"}"
```
Ожидается: `{"token":"dummy-token","username":"admin","role":"ADMIN"}`

**Тест 2: Генерация учетных данных**
```bash
curl -X POST http://localhost:8181/api/users/generate-credentials \
  -H "Content-Type: application/json" \
  -d "{\"firstName\":\"Иван\",\"lastName\":\"Петров\"}"
```
Ожидается: `{"username":"иван.петров","password":"иван.петров###"}`

**Тест 3: Получение списка преподавателей**
```bash
curl http://localhost:8181/api/users/teachers
```
Ожидается: массив пользователей с role="TEACHER"

**Тест 4: Получение статистики**
```bash
curl http://localhost:8181/api/dashboard/stats
```
Ожидается: объект с полями totalStudents, totalGroups, averageGrade, lessonsToday

**Результат:** ✅ API endpoints отвечают корректно

---

## 🔍 Детальное тестирование

### Тестирование форм

#### Создание пользователя
- [ ] Пустые поля не позволяют создать
- [ ] Генерация логина работает
- [ ] Генерация пароля работает
- [ ] Роль выбирается корректно
- [ ] После создания модальное окно закрывается
- [ ] Новый пользователь появляется в списке

#### Создание студента/группы/предмета
- [ ] Все обязательные поля проверяются
- [ ] Данные сохраняются в БД
- [ ] UI обновляется после создания

---

### Тестирование безопасности

#### Аутентификация
- [ ] Без токена нельзя попасть на защищенные страницы
- [ ] Неверный логин/пароль показывает ошибку
- [ ] После logout токен удаляется
- [ ] Пароли не отображаются в логах

#### Авторизация (будущая функция)
- [ ] ADMIN видит все
- [ ] TEACHER видит только свои предметы
- [ ] STUDENT видит только свою успеваемость

---

### Тестирование базы данных

**Подключитесь к PostgreSQL:**
```bash
psql -h localhost -p 5433 -U postgres -d db_students
```

**Проверьте структуру:**
```sql
-- Проверка таблицы users
SELECT * FROM users LIMIT 5;

-- Проверка шифрования паролей
SELECT username, password FROM users WHERE username = 'admin';
-- Пароль должен начинаться с $2a$ или $2b$

-- Проверка ролей
SELECT role, COUNT(*) FROM users GROUP BY role;

-- Проверка связей
SELECT dg.id, s.name as subject, g.name as group_name, u.username as teacher
FROM discipline_group dg
LEFT JOIN subject s ON dg.subject_id = s.id
LEFT JOIN groups g ON dg.group_id = g.id
LEFT JOIN users u ON dg.teacher_id = u.id;
```

---

## 🐛 Тестирование исправлений

### Проверка исправления UserController

**Было:** Неправильная структура файла, imports в неправильном месте  
**Исправлено:** Корректная структура класса

**Как проверить:**
```bash
curl http://localhost:8181/api/users/teachers
curl http://localhost:8181/api/users/students
```
Оба запроса должны возвращать 200 OK и массив пользователей

---

### Проверка исправления DisciplineGroupServiceImpl

**Было:** 
1. Незавершенный метод `getDisciplineGroupById`
2. Отсутствовал метод `saveDisciplineGroup`

**Исправлено:** Оба метода реализованы

**Как проверить:**
1. Создайте связь предмет-группа через UI
2. Проверьте в БД:
```sql
SELECT * FROM discipline_group;
```
3. Попробуйте получить конкретную связь через API

---

## 📊 Тестирование производительности

### Нагрузочное тестирование (опционально)

**Инструмент:** Apache JMeter или Artillery

**Тест 1: Login endpoint**
- 100 одновременных запросов
- Ожидается: все 200 OK, время ответа < 1 сек

**Тест 2: Dashboard stats**
- 50 одновременных запросов
- Ожидается: все 200 OK, время ответа < 2 сек

---

## 🎨 UI/UX тестирование

### Адаптивность

**Desktop (1920x1080):**
- [ ] Dashboard отображается в 4 колонки
- [ ] Все карточки видны без прокрутки
- [ ] Sidebar полностью развернут

**Tablet (768x1024):**
- [ ] Dashboard в 2 колонки
- [ ] Sidebar сворачивается в иконки
- [ ] Модальные окна центрированы

**Mobile (375x667):**
- [ ] Dashboard в 1 колонку
- [ ] Burger menu работает
- [ ] Формы занимают всю ширину

### Проверка в браузерах
- [ ] Chrome (основной)
- [ ] Firefox
- [ ] Edge
- [ ] Safari (если доступен)

---

## ✅ Чеклист перед релизом

### Backend
- [ ] Компиляция без ошибок: `.\mvnw.cmd clean compile`
- [ ] Тесты проходят: `.\mvnw.cmd test` (если есть)
- [ ] application.properties настроен для production
- [ ] Логи не содержат чувствительных данных
- [ ] БД миграции применены

### Frontend
- [ ] Build проходит: `npm run build`
- [ ] Нет eslint ошибок: `npm run lint`
- [ ] Все environment variables настроены
- [ ] API endpoints указывают на production
- [ ] Нет console.log в production коде

### Интеграция
- [ ] Все API endpoints работают
- [ ] CORS настроен правильно
- [ ] Нет 404 ошибок на статику
- [ ] Сессии не теряются
- [ ] Logout полностью очищает данные

---

## 🔧 Troubleshooting

### Backend не запускается
```bash
# Проверьте порт
netstat -ano | findstr :8181

# Проверьте PostgreSQL
psql -h localhost -p 5433 -U postgres -d db_students

# Очистите target
.\mvnw.cmd clean
```

### Frontend не запускается
```bash
# Переустановите зависимости
rm -rf node_modules package-lock.json
npm install

# Проверьте порт
netstat -ano | findstr :5173
```

### API возвращает 401/403
- Проверьте токен в localStorage
- Проверьте SecurityConfig - все /api/** должны быть permitAll()
- Проверьте в Network tab Headers

### Не отображаются данные
- Откройте DevTools → Network
- Проверьте 200 OK от API
- Проверьте Console на ошибки
- Проверьте формат ответа API

---

## 📝 Отчет о тестировании (шаблон)

```markdown
# Отчет о тестировании - [Дата]

## Окружение
- OS: Windows 11
- Java: 17
- Node: 18.x
- PostgreSQL: 18.0
- Browser: Chrome 120

## Результаты

### Smoke Test: ✅ PASSED
- Backend запущен
- Frontend запущен
- Login работает
- Dashboard загружается

### Функциональные тесты: ✅ PASSED (15/15)
- Аутентификация: 3/3
- Управление пользователями: 5/5
- Dashboard: 3/3
- API: 4/4

### UI/UX тесты: ✅ PASSED (8/8)
- Адаптивность: 3/3
- Анимации: 2/2
- Формы: 3/3

### Найденные баги: 0

### Рекомендации:
1. Добавить unit тесты для сервисов
2. Добавить e2e тесты с Playwright
3. Настроить CI/CD pipeline
```

---

## 🎓 Рекомендуемый порядок тестирования

1. **День 1:** Smoke test + API тесты (1 час)
2. **День 2:** Функциональные тесты UI (2 часа)
3. **День 3:** Тестирование БД + безопасность (2 часа)
4. **День 4:** UI/UX + адаптивность (2 часа)
5. **День 5:** Регрессионное тестирование (1 час)

**Итого:** ~8 часов полного тестирования

---

## 📞 Контакты

При обнаружении критических багов:
1. Остановите оба сервера
2. Сохраните логи (backend и browser console)
3. Запишите шаги воспроизведения
4. Проверьте в TEST_CASES.md известные проблемы
